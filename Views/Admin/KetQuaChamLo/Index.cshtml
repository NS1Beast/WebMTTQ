@model IEnumerable<WebMTTQ.Models.KetQuaChamLo>
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "Kết quả chăm lo";
}

<div class="admin-page-header">
    <div>
        <h2>Lịch sử Chăm lo An sinh Xã hội</h2>
        <div class="subtitle">Quản lý các hoạt động chăm lo an sinh xã hội</div>
    </div>
    <a href="/admin/ketquachamlo/Create" class="admin-btn admin-btn-primary">
        <i class="fas fa-plus"></i> Thêm hoạt động mới
    </a>
</div>

<div class="admin-card">
    <div class="admin-card-body p-0">
        <div class="admin-table-wrap">
            <table class="admin-table">
                <thead>
                    <tr>
                        <th class="cell-center">Tháng</th>
                        <th>Đơn vị thực hiện</th>
                        <th>Nội dung</th>
                        <th class="cell-center">SL</th>
                        <th class="cell-right">Kinh phí</th>
                        <th class="cell-actions">Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        foreach (var item in Model)
                        {
                            <tr>
                                <td class="cell-center">
                                    <span class="admin-badge red">Tháng @item.Thang</span>
                                </td>
                                <td>
                                    <strong>@item.DonViUngHo</strong>
                                    <div style="font-size:0.78rem;color:var(--admin-muted);">Nhóm: @item.PhanLoaiDonVi</div>
                                </td>
                                <td>@item.NoiDung</td>
                                <td class="cell-center">@item.SoLuongHo</td>
                                <td class="cell-right"><span class="admin-money red">@item.KinhPhi.ToString("N0")đ</span></td>
                                <td class="cell-actions">
                                    <a href="/admin/ketquachamlo/Edit/@item.Id" class="admin-btn admin-btn-warning admin-btn-sm">
                                        <i class="fas fa-edit"></i> Sửa
                                    </a>
                                    <form action="/admin/ketquachamlo/Delete/@item.Id" method="post" style="display:inline;" onsubmit="return confirm('Bạn có chắc chắn muốn xóa hoạt động này?');">
                                        <button type="submit" class="admin-btn admin-btn-danger admin-btn-sm">
                                            <i class="fas fa-trash"></i> Xóa
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="6">
                                <div class="admin-empty">
                                    <i class="fas fa-hand-holding-heart"></i>
                                    <p>Chưa có dữ liệu chăm lo nào.</p>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>